apiVersion: v1
kind: Secret
metadata:
  name: livekit-minio-credentials
  namespace: {{ .Release.Namespace }}
type: Opaque
# This secret is now generated by secret-generators.yaml
# Remove this template and use the ExternalSecret instead
{{- if false }}
stringData:
  # This template is deprecated - use ExternalSecret from secret-generators.yaml
  LIVEKIT_S3_ACCESS_KEY: "deprecated"
  LIVEKIT_S3_SECRET: "deprecated"
  S3_ACCESS_KEY: "deprecated" 
  S3_SECRET_KEY: "deprecated"
{{- end }}